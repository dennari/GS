<UserControl
    x:Class="Growthstories.UI.WindowsPhone.AddPhotographView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:local="clr-namespace:Growthstories.UI.WindowsPhone"
    mc:Ignorable="d" Width="480" Height="800">

    <local:AddPlantActionView>
        <StackPanel x:Name="ContentPanel" Grid.Row="1" Margin="24,0">
            <Button
            Height="200"                   
            Command="{Binding PhotoChooserCommand, Mode=OneWay}"                    
            >
                <Button.Background>
                    <ImageBrush 
                        Stretch="UniformToFill" 
                        ImageSource="{Binding Photo}"/>
                </Button.Background>
            </Button>

        </StackPanel>

    </local:AddPlantActionView>
</UserControl>