<phone:PhoneApplicationPage
    x:Class="GrowthStories.UI.WindowsPhone.Views.PlantView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:phone="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone"
    xmlns:shell="clr-namespace:Microsoft.Phone.Shell;assembly=Microsoft.Phone"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:i="clr-namespace:System.Windows.Interactivity;assembly=System.Windows.Interactivity" 
    xmlns:command="clr-namespace:GalaSoft.MvvmLight.Command;assembly=GalaSoft.MvvmLight.Platform.WP8"
    xmlns:ec="clr-namespace:Microsoft.Expression.Interactivity.Core;assembly=Microsoft.Expression.Interactions"
    xmlns:bar="clr-namespace:BindableApplicationBar;assembly=BindableApplicationBar"
    xmlns:local="clr-namespace:GrowthStories.UI.WindowsPhone"
    DataContext="{Binding PlantVM, Source={StaticResource Locator}}"
    SupportedOrientations="Portrait" Orientation="Portrait"
    mc:Ignorable="d"
    shell:SystemTray.IsVisible="True">

    <phone:PhoneApplicationPage.Resources>
        <local:TestUrl x:Key="TestUrl" />
    </phone:PhoneApplicationPage.Resources>
    <!--LayoutRoot is the root grid where all page content is placed-->
    <Grid x:Name="LayoutRoot" Background="Transparent">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!--TitlePanel contains the name of the application and page title-->
        <StackPanel Grid.Row="0" Margin="12,17,0,28">
            <TextBlock Text="GROWTH STORIES" Style="{StaticResource PhoneTextNormalStyle}" x:Name="apptitle"/>
            <TextBlock 
                Text="{Binding Plant.Name}" 
                Margin="9,-7,0,0" 
                Style="{StaticResource PhoneTextTitle1Style}" 
                />
        </StackPanel>



        <!--ContentPanel - place additional content here-->
        <Grid x:Name="ContentPanel" Grid.Row="1" Margin="12,0,12,0">
            <phone:LongListSelector ItemsSource="{Binding Actions}" >
                <phone:LongListSelector.ItemTemplate>
                    <DataTemplate>
 
                        <Border BorderBrush="#B2FFFFFF" BorderThickness="1" Padding="0,12">
                            <i:Interaction.Triggers>
                                <i:EventTrigger EventName="Tap">
                                    <command:EventToCommand Command="{Binding GardenVM.ShowDetailsCommand, Mode=OneWay, Source={StaticResource Locator}}"
                                                CommandParameter="{Binding Mode=OneWay}" />
                                </i:EventTrigger>
                            </i:Interaction.Triggers>
                            <StackPanel FlowDirection="LeftToRight" Orientation="Horizontal" >
                                <TextBlock Text="{Binding Converter={StaticResource ActionToDateString}}" VerticalAlignment="Center" Style="{StaticResource PhoneTextNormalStyle}" FontSize="14"/>
                                <TextBlock Text="{Binding Converter={StaticResource ActionToString}}" VerticalAlignment="Center" Style="{StaticResource PhoneTextNormalStyle}" FontSize="26"/>

                            </StackPanel>

                        </Border>
                    </DataTemplate>
                </phone:LongListSelector.ItemTemplate>
            </phone:LongListSelector>
        </Grid>
    </Grid>
    <bar:Bindable.ApplicationBar>
        <bar:BindableApplicationBar>
            <bar:BindableApplicationBarButton                    
                        Text="Water"
                        IconUri="/Assets/AppBar/Add.png"
                        Command="{Binding AddWaterCommand}"/>
            <bar:BindableApplicationBarButton                    
                        Text="Comment"
                        IconUri="/Assets/AppBar/Add.png"
                        Command="{Binding AddCommentCommand}" CommandParameter="NOTE"/>
            <bar:BindableApplicationBarButton                    
                        Text="Photo"
                        IconUri="/Assets/AppBar/Add.png"
                        Command="{Binding AddPhotoCommand}" CommandParameter="{StaticResource TestUrl}"/>
            <bar:BindableApplicationBarButton                    
                        Text="Fertilizer"
                        IconUri="/Assets/AppBar/Add.png"
                        Command="{Binding AddFertilizerCommand}"/>
        </bar:BindableApplicationBar>
    </bar:Bindable.ApplicationBar>

</phone:PhoneApplicationPage>