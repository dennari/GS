<UserControl
    x:Class="Growthstories.UI.WindowsPhone.Design.PlantPhotographActionView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:local="clr-namespace:Growthstories.UI.WindowsPhone.Design"
    mc:Ignorable="d" Width="480" Height="800" 
    d:DataContext="{StaticResource PlantPhotoViewModel}"
    HorizontalAlignment="Stretch"
    VerticalAlignment="Stretch"
    >

    <Grid HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
        <Grid.Background>
            <ImageBrush
                    ImageSource="{Binding PhotoData, Converter={StaticResource PhotoToImageSource}}" Stretch="Uniform"/>
        </Grid.Background>
    </Grid>

</UserControl>