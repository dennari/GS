<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:converters="clr-namespace:Growthstories.WP8.Converters" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    mc:Ignorable="d">

    <converters:ImagePathConverter x:Key="ImagePathConverter"/>


    <DataTemplate x:Key="GardenPlantTemplate">
        <Grid Width="202" Height="202">
            <Image x:Name="ProfilePicture" Source="{Binding Path='', Converter={StaticResource ImagePathConverter}}" Width="100" Height="100"/>
            
            <TextBlock HorizontalAlignment="Center" Margin="0,0,0,12" TextWrapping="Wrap" Text="{Binding Name}" VerticalAlignment="Bottom" Foreground="{StaticResource PhoneForegroundBrush}" Style="{StaticResource PhoneTextNormalStyle}"/>
            <TextBlock HorizontalAlignment="Center" Margin="0,12,0,0" TextWrapping="Wrap" Text="{Binding Genus}" VerticalAlignment="Top" Foreground="{StaticResource PhoneSubtleBrush}" Style="{StaticResource PhoneTextSmallStyle}" FontSize="16"/>
            <!--<TextBlock Text="OOOOH"/>-->
        </Grid>
    </DataTemplate>


</ResourceDictionary>
